# 青空进学塾运营管理系统

## 更新记录（倒序）




























































### v8.3.11
- 月度结算右侧重新显示“已收学费（日元）”。
- 课时管理和月度结算的课时排序统一改为固定课程顺序。
- 固定课程顺序：
  - 日语
  - 数学
  - 文综/综合科目
  - 物理
  - 化学
  - 生物
- 排序整体为：月份 → 老师 → 固定课程顺序 → 日期 → 时间。
- 主界面版本号完整显示为 v8.3.11。
- 本版无 SQL 修改。

### v8.3.10
- 课时管理继续微调列宽。
- 勾选列缩小到更细，只保留 checkbox。
- 科目列增加宽度；科目列不再显示金额，只显示科目、时间和时长。
- 内容列保持固定宽度，长文本只截断显示，不再撑高行。
- 月度结算课时明细标题修正为：日期、姓名、担当老师、科目、状态、内容、操作。
- 月度结算每条课时增加编辑、删除按钮，竖向排列。
- 月度结算科目列分三行显示：科目、时间/时长、金额。
- 月底实际结算重新显示“已收学费（日元）”。
- 主界面版本号完整显示为 v8.3.10。
- 本版无 SQL 修改。

### v8.3.9
- 课时管理一览改为每侧 8 列：
  - 勾选
  - 日期
  - 姓名
  - 担当老师
  - 科目
  - 状态
  - 内容
  - 操作
- 勾选 checkbox 移到最左侧细列。
- 日期、姓名、担当老师、状态列进一步压缩。
- 上课内容独立为状态右侧的一列，宽度接近状态列。
- 操作按钮独立成最后一列，避免撑高内容列。
- 主界面版本号完整显示为 v8.3.9。
- 本版无 SQL 修改。

### v8.3.8
- 月度结算界面不再显示预设汇率。
- 月度结算左右两块摘要表调整为相同行数。
- 右侧摘要表隐藏“已收学费（日元）”，保留“已收学费（人民币）”。
- 主界面版本号完整显示为 v8.3.8。
- 本版无 SQL 修改。

### v8.3.7
- 说明：上一版看起来没有变化，是因为旧版课时渲染函数和 CSS 覆盖层太多，追加样式没有真正接管输出。
- 本版直接重写课时管理一览渲染函数，不再只靠追加 CSS。
- 日期、姓名、担当老师、状态/计费列缩窄，内容/操作列加宽。
- 操作按钮区域固定为紧凑网格。
- 学生管理预设汇率继续保持空白默认值，并允许小数点后 4 位。
- 再次强制去除月度结算内容单元格的双层网格线。
- 主界面版本号完整显示为 v8.3.7。
- 本版无 SQL 修改。

### 更早版本
- 旧版详细更新记录已从 README 中省略。
- 如需查看历史改动，请通过 GitHub commit history 查询。
