# 青空进学塾运营管理系统

## 更新记录（倒序）









































































































































































### v9.6.0
- 新增老师勤务申报模板导出。
- 在老师工资结算页面新增按钮：
  - `导出勤务申报模板`
- 导出文件为 Excel 可打开的 `.xls` 模板。
- 选择具体老师时：
  - 导出该老师当前工资结算月份的勤务申报表
- 未选择老师时：
  - 按老师分别导出当前月份所有老师的勤务申报表
- 模板包含：
  - 月份
  - 老师姓名
  - 日期及星期
  - 工作内容
  - 开始时间
  - 结束时间
  - 时长
  - 当日交通费
  - 当日教室费
  - 备注
  - 日元银行振込信息
  - 支付宝 / 微信信息
- 模板不包含以下内部保密信息：
  - 业务归属
  - 时给
  - 课时工资
  - 系统计算工资
  - 学生收费金额
- 本版先做导出，不做导入核对。
- 本版无 SQL 修改。
- 本版压缩包不包含旧 SQL 文件。

### v9.5.0
- 首页统计增加年度数据。
- 首页新增：
  - 本年度收入
  - 本年度普通支出
  - 本年度老师工资
  - 本年度净收入
- 收支汇总页面升级为月度 + 年度统计。
- 收支汇总新增年份筛选。
- 收支汇总新增：
  - 老师工资
  - 净收入
  - 年度按月份明细表
- 年度明细表显示：
  - 月份
  - 收入
  - 普通支出
  - 老师工资
  - 净收入
  - 收入记录数
  - 支出记录数
- 统计规则：
  - 收入：收入记录中 `status = received`
  - 普通支出：支出记录中 `status = paid / reimbursed`
  - 普通支出排除 `expense_category = teacher_wage`
  - 老师工资：支付要求中 `source_type = teacher_wage` 且未作废/未取消
  - 净收入 = 收入 - 普通支出 - 老师工资
- 工资统计以支付管理的支付要求为准，和已确认的工资锁定金额保持一致。
- 本版无 SQL 修改。
- 本版压缩包不包含旧 SQL 文件。

### v9.4.5
- 优化老师工资锁定按钮逻辑。
- 按当前筛选范围判断锁定状态，而不是只要当前月份有锁定就禁用按钮。
- 选择具体老师时：
  - 只判断该老师当前月份是否已锁定
  - 该老师未锁定时，锁定按钮可用
  - 该老师已锁定时，锁定按钮不可用、撤销按钮可用
- 未选择老师时：
  - 点击锁定会锁定当前月份所有未锁定老师
  - 已锁定老师自动跳过
  - 如果所有显示范围都已锁定，锁定按钮不可用
- 撤销逻辑：
  - 选中老师时，只撤销该老师当前月份工资锁定
  - 未选老师时，撤销当前月份当前显示范围内的工资锁定
- 按钮文案调整为：
  - 锁定当前显示工资
  - 撤销当前显示锁定
- 本版无 SQL 修改。
- 本版压缩包不包含旧 SQL 文件。

### v9.4.4
- 优化课时编辑保存失败时的错误提示。
- 如果因为学生月度结算锁定或老师工资锁定导致不能保存：
  - 不关闭编辑弹窗
  - 在弹窗内部显示完整错误信息
  - 同时保留页面顶部提示
- 增加已配对实际课时的归属月份保护。
- 已关联预定课时的实际课时，不允许单独修改归属月份。
- 防止出现实际课时仍关联 2 月预定课时，但归属月份被改到 3 月，导致在月度结算画面中看起来“消失”。
- 如需调整这类课时月份，建议删除/撤销实际课时后，从正确月份的预定课时重新生成。
- 本版无 SQL 修改。
- 本版压缩包不包含旧 SQL 文件。

### v9.4.3
- 加强课时编辑锁定保护。
- 编辑实际课时时，如果修改会影响已锁定的学生月度结算，禁止保存。
- 受学生月度结算锁定保护的字段包括：
  - 学生
  - 归属月份
  - 课时类型
  - 上课日期
  - 科目
  - 课时时长
  - 单价
  - 课时费
  - 是否计费
  - 状态
  - 业务归属
  - 学生结算月份
- 编辑实际课时时，如果修改会影响已锁定的老师工资结算，禁止保存。
- 受老师工资锁定保护的字段包括：
  - 老师
  - 工资结算月份
  - 上课日期
  - 开始时间
  - 结束时间
  - 课时时长
  - 实际分钟
  - 工资课时
  - 科目
  - 学生
  - 业务归属
  - 状态
- 如果把工资结算月份改到另一个已锁定工资月份，也会禁止保存。
- 本版无 SQL 修改。
- 本版压缩包不包含旧 SQL 文件。

### v9.4.2
- 修复学生月度结算切换月份后，底部绿色锁定信息残留的问题。
- 学生月度结算页面：
  - 切换月份或学生时，立即清空旧锁定状态显示
  - 重新读取当前月份 / 当前学生的锁定状态
  - 防止旧月份的异步读取结果覆盖新月份画面
- 老师工资结算本版不再改动。
- 支付管理本版不再改动。
- 本版无 SQL 修改。
- 本版压缩包不包含旧 SQL 文件。

### v9.4.1
- 修复学生月度结算撤销后，底部绿色“已锁定”信息仍残留的问题。
  - 锁定状态读取只认 `settlement_status = locked`
  - 撤销后显示“当前月份尚未锁定”
  - 锁定 / 撤销按钮状态同步刷新
- 修复老师工资结算锁定结果刷新问题。
  - 切换工资月份后，底部锁定结果立即按当前月份刷新
  - 切换老师后，底部锁定结果立即按当前老师刷新
  - 不再保留上一月份的锁定结果显示
- 工资锁定撤销后：
  - 对应支付要求作废
  - 如果支付管理页面已打开，会刷新支付要求列表
- 说明：
  - 学生月度结算锁定和老师工资锁定是两套独立业务。
  - 撤销学生月度结算不会自动撤销老师工资锁定。
  - 要取消工资支付要求，需要在老师工资结算中撤销对应月份工资锁定。
- 本版无 SQL 修改。
- 本版压缩包不包含旧 SQL 文件。

### v9.4.0
- 支付管理与实际支出记录联动。
- 支付管理点击“标记已支付”时，不再直接改状态，而是打开支付确认窗口。
- 支付确认窗口支持：
  - 支付账户
  - 支付日期
  - 支付金额
  - 日元金额
  - 人民币金额
  - 备注
- 确认支付后：
  - 自动生成一条支出记录
  - 支出分类为 `teacher_wage`
  - 支出记录不计入学生月度结算
  - 支付要求状态改为已支付
  - 记录 paid_at
  - 对应账户余额减少
- 账户余额重算逻辑修正：
  - 按账户币种使用 amount / amount_jpy / amount_cny
  - 避免跨币种收入支出导致余额计算错误
- 本版无 SQL 修改。
- 本版压缩包不包含旧 SQL 文件。

### v9.3.4
- 修正课时管理页面的月份联动筛选。
- 课时管理选择月份后：
  - 学生下拉框只显示该月有课时记录的学生
  - 老师下拉框只显示该月有课时记录的老师
  - 科目下拉框只显示该月有课时记录的科目
  - 如果原选中对象不属于新月份，会自动清空
- 继续保留 v9.3.3 的：
  - 学生月度结算按月份过滤学生
  - 老师工资结算按月份过滤老师
- 修复 Manifest 图标报错：
  - `manifest.json` 引用增加版本号，避免浏览器继续读取旧 manifest
  - Manifest 图标改为 `assets/favicon-192.png?v=9.3.4` 和 `assets/favicon-512.png?v=9.3.4`
  - 不再引用根目录 `logo-vertical.png`
- 本版无 SQL 修改。
- 本版压缩包不包含旧 SQL 文件。

### v9.3.3
- 按月份过滤结算对象下拉框。
- 学生月度结算：
  - 选择月份后，学生下拉框只显示该月有课时记录的学生
  - 如果该月没有课时学生，显示“该月无课时学生”
  - 切换月份后，如果原选中学生不属于该月，会自动清空
- 老师工资结算：
  - 选择工资结算月份后，老师下拉框只显示该月有实际课时的老师
  - 使用老师工资结算月份判断，不单纯按上课日期判断
  - 如果该月没有实际课时老师，显示“该月无实际课时老师”
  - 切换月份后，如果原选中老师不属于该月，会自动清空
- 修复 Manifest 图标 404：
  - Manifest 不再引用根目录 `logo-vertical.png`
  - 改为引用 `assets/favicon-192.png` 和 `assets/favicon-512.png`
- 本版无 SQL 修改。
- 本版压缩包不包含旧 SQL 文件。

### v9.3.2
- 调整工资锁定按钮活性控制。
- 老师工资结算：
  - 未锁定时：`锁定当前工资` 可用，`撤销当前锁定` 不可用
  - 已锁定时：`锁定当前工资` 不可用，`撤销当前锁定` 可用
- 学生月度结算：
  - 新增 `撤销本月锁定` 按钮
  - 未锁定时：确认锁定按钮可用，撤销按钮不可用
  - 已锁定时：确认锁定按钮不可用，撤销按钮可用
- 撤销学生月度结算锁定后，可重新修改对应课时和学费收入记录。
- 本版无 SQL 修改。
- 本版压缩包不包含旧 SQL 文件。

### v9.3.1
- 支付管理默认隐藏“已作废”支付要求。
- 状态筛选的默认项改为“全部有效状态”。
- 默认统计范围：
  - 待支付
  - 已支付
  - 已取消
- 默认不包含：
  - 已作废
- 只有状态筛选明确选择“已作废”时，才显示作废记录。
- 筛选合计、记录数默认不再统计已作废金额。
- 本版无 SQL 修改。
- 本版压缩包不包含旧 SQL 文件。

### v9.3.0
- 新增支付管理页面。
- 支付管理显示 v9.2 工资锁定后生成的待支付要求。
- 支持筛选：
  - 月份
  - 状态
  - 来源
  - 业务归属
  - 币种
- 顶部统计：
  - 待支付合计
  - 已支付合计
  - 筛选合计
  - 记录数
- 支持操作：
  - 标记已支付
  - 取消支付要求
  - 恢复待支付
- 本版只管理支付要求状态，不生成实际账户流水。
- 实际支付记录 / 账户余额联动放到后续版本。
- 本版无 SQL 修改。
- 本版压缩包不包含旧 SQL 文件。

### v9.2.2
- 整理 logo 文件结构。
- 根目录的 `logo-horizontal.png`、`logo-vertical.png` 移动到：
  - `assets/logo-horizontal.png`
  - `assets/logo-vertical.png`
- 新增不带文字的图形 logo：
  - `assets/logo-mark.png`
- 网站标签页 favicon 改为不带文字的图形 logo。
- 生成 favicon 相关文件：
  - `favicon.ico`
  - `assets/favicon-32.png`
  - `assets/favicon-48.png`
  - `assets/favicon-180.png`
  - `assets/favicon-192.png`
  - `assets/favicon-512.png`
- 已更新前端引用路径。
- 本版无 SQL 修改。
- 本版压缩包不包含旧 SQL 文件。

### v9.2.1
- 增加学生月度结算锁定保护。
- 学生月度结算已锁定后，禁止直接修改或删除：
  - 该学生该月相关课时
  - 该学生该月学费收入记录
- 收入记录的删除选中 / 删除本月记录中，如果包含已锁定学生结算的学费记录，也会阻止删除。
- 普通收入、普通支出不受学生月度结算锁定影响。
- 网站标签页 favicon 改为青空进学塾竖版 logo 图标。
- 本版无 SQL 修改。
- 本版压缩包不包含旧 SQL 文件。

### v9.2.0
- 新增老师工资锁定 / 支付准备基础功能。
- 老师工资结算页面底部新增：
  - 锁定当前工资
  - 撤销当前锁定
  - 刷新锁定结果
  - 已锁定工资结果表
- 锁定时保存当前工资结算快照：
  - 老师
  - 工资结算月份
  - 业务归属
  - 结算方式
  - 工资课时
  - 课时工资
  - 交通费 / 教室费
  - 日元合计
  - 人民币合计
  - 明细课时来源
- 锁定时自动生成支付管理用的待支付数据。
- `不计工资` 的工资规则：
  - 会保存锁定结果
  - 不生成待支付要求
- 支持撤销锁定：
  - 锁定结果改为已撤销
  - 对应待支付要求作废
- 删除保护：
  - 老师工资锁定后，对应老师、工资月份的实际课时不允许直接删除
  - 需要先撤销工资锁定再删除
- 新增 SQL：
  - `school_v9_2_0_teacher_wage_lock.sql`
- 本版需要先运行 SQL。
- 本版压缩包只包含本次新增 SQL，不包含旧 SQL 文件。

### v9.1.15
- 老师工资结算明细增加可手动输入的：
  - 交通费
  - 教室费
- 交通费、教室费按每节课逐行录入。
- 金额计算：
  - 课时工资 = 工资课时 × 时给
  - 合计（日元） = 课时工资（日元） + 交通费 + 教室费
  - 人民币折算 = 课时工资人民币折算 + 交通费/教室费按汇率折算
- 顶部预计工资合计改为包含交通费、教室费后的合计。
- 汇总表的预计合计显示：
  - 合计日元
  - 合计人民币
  - 课时工资 / 费用拆分
- 本版的交通费、教室费暂不保存数据库，刷新页面后恢复 0。
- 本版无 SQL 修改。
- 本版压缩包不包含旧 SQL 文件。

### v9.1.14.1
- 修正老师工资规则页面“复制”按钮大小。
- 工资规则表格中的“复制”按钮现在和同一行的“编辑 / 删除”按钮尺寸一致。
- 不影响收入记录、支出记录的复制按钮。
- 本版无 SQL 修改。
- 本版压缩包不包含旧 SQL 文件。

### v9.1.14
- 每条记录的操作区增加“复制”按钮。
- 当前对象：
  - 老师工资规则
  - 收入记录
  - 支出记录
- 复制规则：
  - 复制不会直接保存新记录。
  - 收入/支出：以当前记录内容打开新增画面，确认后手动保存。
  - 老师工资规则：把当前规则内容复制到上方输入区，确认后手动保存。
- 复制时不会带入原记录的 `id / created_at / updated_at`。
- 本版无 SQL 修改。
- 本版压缩包不包含旧 SQL 文件。

### v9.1.13
- 老师工资规则的结算方式增加：
  - `不计工资`
- 用途：
  - 老板本人授课
  - 不需要进入老师工资支付的课时
- 工资结算页面规则匹配到“不计工资”时：
  - 规则状态显示“不计工资”
  - 时给显示 `-`
  - 日元工资为 0
  - 人民币折算为 0
- 后续生成支付要求时，可按该结算方式自动跳过。
- 本版无 SQL 修改。
- 本版压缩包不包含旧 SQL 文件。

### v9.1.12
- 在已增加 checkbox 的页面追加“删除选中记录”功能。
- 修改对象：
  - 老师工资规则
  - 收入记录
  - 支出记录
- 按钮文案调整：
  - 老师工资规则：`清空` → `清空输入内容`
  - 收入记录：新增 `删除选中记录`
  - 支出记录：新增 `删除选中记录`
  - 收入/支出原 `删除筛选...` → `删除本月记录`
- 说明：
  - `清空输入内容` 只清空上方表单，不删除表格数据。
  - `删除选中记录` 只删除 checkbox 勾选的记录。
  - `删除本月记录` 沿用原本按当前筛选条件批量删除的逻辑，只是文案更明确。
- 本版无 SQL 修改。
- 本版压缩包不包含旧 SQL 文件。

### v9.1.11
- 给逐条录入页面增加 checkbox 选择列。
- 当前追加对象：
  - 老师工资规则
  - 收入记录
  - 支出记录
- 每条记录增加一个 checkbox。
- 表头增加全选 checkbox。
- 月份分组行自动调整 colspan，避免表格错位。
- 本版只增加 UI 选择，不改变保存、删除、导入等业务逻辑。
- 暂不影响：
  - 账户管理
  - 业务归属
  - 学生管理
- 本版无 SQL 修改。

### v9.1.10.4
- 修复工资规则仍无法匹配的情况。
- 保留原有 ID 精确匹配。
- 新增名称兜底匹配：
  - 老师名
  - 学生名
  - 科目名
  - 业务归属名
- 适用于课时记录和工资规则显示名称一致，但内部 ID 不一致或旧数据 ID 缺失的情况。
- 如果使用名称兜底匹配，页面规则列会显示“名称匹配”。
- 本版不修改 HTML 大结构。
- 本版无 SQL 修改。

### v9.1.10.3
- 修复老师工资规则设置后仍然无法匹配的问题。
- 工资规则匹配不再直接读取课时记录的原始字段，而是统一取：
  - 老师 ID
  - 学生 ID
  - 科目 ID
  - 业务归属 ID
- 业务归属 ID 取得优先级：
  - 课时记录自身 `business_entity_id`
  - 课时记录嵌套 `business_entity.id`
  - 学生资料中的 `business_entity_id`
- 规则未设置提示也改为使用实际匹配用的组合。
- 刷新计算时继续重新读取工资规则。
- 不修改 HTML 大结构。
- 本版无 SQL 修改。

### v9.1.10.2
- 安全修复老师工资结算汇总表表头与内容不匹配的问题。
  - 不再直接替换 `index.html` 的大段表格结构。
  - 改为在老师工资页面渲染时，只定位 `teacherWageSummaryTable` 所在表格并更新表头。
- 修复汇率查询 CORS 报错：
  - 去掉 Frankfurter API。
  - 改用 `open.er-api.com` 优先。
  - 使用 jsdelivr currency API 作为备用。
- 本版无 SQL 修改。

### v9.1.10
- 优化老师工资结算页面的显示与排序。
- 工资汇总表增加：
  - 业务归属
  - 学生
  - 结算方式
  - 规则状态
- 汇总和明细排序优先级调整为：
  - 业务归属
  - 老师
  - 学生
  - 科目
  - 日期
  - 时间
- 规则未设置时，显示缺少的规则组合：
  - 老师 / 学生 / 科目 / 业务归属
- 解决同一业务归属看起来被拆散、不容易判断规则缺失的问题。
- 本版无 SQL 修改。

### v9.1.9
- 将老师工资规则接入老师工资结算明细。
- 工资规则按以下条件匹配：
  - 老师
  - 学生
  - 科目
  - 业务归属
- 工资明细新增显示：
  - 业务归属
  - 结算方式
  - 时给
  - 课时工资（日元）
  - 人民币折算
  - 规则匹配状态
- 顶部预计工资合计显示：
  - 日元合计
  - 人民币合计
- 工资课时手动调整后，金额实时重算。
- 如果没有匹配工资规则，明细行显示“未设置”。
- 本版不处理交通费/教室费；后续在工资明细中逐行维护。
- 本版无 SQL 修改。

### v9.1.8.3
- 老师工资规则页面的“汇率”输入框旁增加：
  - 查询今日汇率
- 点击后查询 JPY → CNY 当天汇率，并自动填入汇率输入框。
- 查询后仍可手动修改。
- 保存时仍然只保存原有 `exchange_rate` 字段。
- 不额外保存来源、查询时间。
- 本版无 SQL 修改。

### v9.1.8.2
- 老师工资规则中去除：
  - 交通费/节
  - 教室费/节
- 原因：
  - 交通费、教室费与实际课时关系更强，金额不稳定，不适合作为固定工资规则。
- 工资规则保留：
  - 老师
  - 学生
  - 科目
  - 业务归属
  - 结算方式
  - 日元时给
  - 人民币时给
  - 汇率
  - 启用状态
  - 备注
- 交通费、教室费后续将在老师工资结算明细中逐行维护。
- 本版提供说明 SQL：
  - `school_v9_1_8_2_teacher_wage_rules_no_fees.sql`
- 如果之前已经运行 v9.1.8/9.1.8.1，本 SQL 可不运行。

### v9.1.8.1
- 老师工资规则增加“学生”维度。
- 工资规则改为按：
  - 老师
  - 学生
  - 科目
  - 业务归属
  - 结算方式
  区分。
- 适用于同一老师给不同学生上同一科目时时给不同的情况。
- 新增 SQL：
  - `school_v9_1_8_1_teacher_wage_rules_student.sql`
- 本版需要先运行 SQL。
- 本版仍只做规则维护，暂不接入老师工资结算计算。

### v9.1.8
- 新增老师工资规则基础版。
- 新增页面：
  - 老师工资规则
- 可以维护：
  - 老师
  - 科目
  - 业务归属
  - 结算方式：日元时薪 / 人民币时薪
  - 日元时给
  - 人民币时给
  - 汇率
  - 交通费/节
  - 教室费/节
  - 启用状态
  - 备注
- 本版只做规则维护，不接入老师工资结算计算。
- 下一版计划将规则接入老师工资明细。
- 新增 SQL：
  - `school_v9_1_8_teacher_wage_rules.sql`
- 本版需要先运行 SQL。

### v9.1.7
- 完整课时导入前必须选择课时管理顶部月份。
- 如果没有选择月份：
  - 禁止导入。
  - 提示先选择模板对应年月。
- 如果已选择月份：
  - 导入前显示确认提示。
  - 系统按该月份的年份解析模板日期。
- 用途：
  - 避免 `3.2周`、`3.30周`、`4.1` 这类模板日期在未选择年份时解析错误。
- 工资结算月份仍以模板“工资结算月份”列为准。
- 本版无 SQL 修改。

### v9.1.6
- 完整课时导入支持读取“工资结算月份”列。
- 支持格式：
  - `2026.4`
  - `2026.04`
  - `2026-04`
  - `2026/04`
  - `2026年4月`
- 只写入实际课时：
  - `teacher_settlement_month`
- 预定课时不写入工资结算月份。
- 如果模板列为空：
  - 默认使用实际上课日期所在月份。
  - 再没有则使用学生收费归属月份。
- 本版无新增 SQL；继续使用 v9.1.5 的 `teacher_settlement_month` 字段。

### v9.1.5
- 新增实际课时的“工资结算月份”。
- 只针对实际课时：
  - 预定课时不显示、不保存工资结算月份。
  - 实际课时新增/编辑时显示“工资结算月份”。
- 默认规则：
  - 工资结算月份 = 上课日期所在月份。
- 老师工资结算页面改用：
  - `teacher_settlement_month` 作为月份筛选依据。
- 旧数据兼容：
  - 如果 `teacher_settlement_month` 为空，则使用 `lesson_date` 所在月份。
  - 再为空时使用 `year_month`。
- 新增 SQL：
  - `school_v9_1_5_teacher_settlement_month.sql`
- 本版需要先运行 SQL。

### v9.1.4
- 老师工资结算明细增加“工资课时”列。
- 默认工资课时：
  - 实际授课分钟按30分钟向下取整。
  - 例：131分钟 → 2H，150分钟 → 2.5H。
- 支持在明细行手动调整工资课时。
- 调整后：
  - 汇总表实时重算。
  - 顶部工资课时和预计工资实时重算。
- 本版暂不保存数据库。
  - 刷新页面后恢复默认计算值。
- 本版无 SQL 修改。

### v9.1.3
- 修复老师工资结算页面集成包内容仍为 v9.0.6 的问题。
- 已验证压缩包内 `index.html` 与 `app.js` 均为 v9.1.3。
- 新增左侧菜单和页面：老师工资结算。
- 本版无 SQL 修改。

### v9.0.6
- 修复手动新增/编辑课时时，时长输入框不能输入小数的问题。
- 时长字段改为允许小数：
  - `step="0.01"`
  - 支持 1.25 / 1.5 / 2.08 等输入
- 保留 v9.0.5 的自动计算：
  - 开始时间 + 结束时间 → 时长
  - 课程单价 × 时长 → 应收课时费
- 仍然允许手动修正时长和课时费。
- 修改位置：
  - `js/modules/lessons.js`
- 本版无 SQL 修改。

### v9.0.5
- 手动新增/编辑课时时，支持根据开始时间和结束时间自动计算时长。
- 自动计算规则：
  - 开始时间 + 结束时间 → 时长（H）
  - 课程单价 × 时长 → 应收课时费
- 支持手动修正：
  - 用户手动修改时长后，不再强制覆盖时长
  - 用户手动修改应收课时费后，不再强制覆盖课时费
- 本功能只影响手动新增/编辑课时。
- 不修改 Excel 导入逻辑。
- 不修改课时列表显示。
- 不修改月度结算统计口径。
- 修改位置：
  - `js/modules/lessons.js`
- 本版无 SQL 修改。

### v9.0.4
- 新增/编辑课时弹窗增加“回数”输入框。
  - 用于补录旧课时记录的第几回。
  - 不强制必填。
  - 保存时写入 `lesson_count`。
- 修复旧版回数字段插入位置不适配当前表单结构的问题。
- 修复收入记录顶部统计栏显示为 0 的问题。
  - 兼容 `currency` / `payment_currency`
  - 兼容 `amount` / `payment_amount`
  - 兼容 `received` / `已收`
- 不修改课时表格结构。
- 不修改月度结算统计口径。
- 修改位置：
  - `js/modules/lessons.js`
  - `js/modules/income.js`
- 本版无 SQL 修改。

### v9.0.3
- 修改课时排序和月度结算明细显示。
- 课时排序规则统一为：
  - 月份
  - 科目优先级
  - 老师
  - 日期
  - 回数
  - 开始时间
- 该排序同时用于：
  - 课时管理
  - 学生月度结算明细
- 月度结算明细的日期列增加“第X回”显示。
- 不改课时表格 HTML，不改课时统计，不改结算统计。
- 修改位置：
  - `js/modules/lessons.js`
  - `js/modules/student-settlement.js`
- 本版无 SQL 修改。

### v9.0.2
- 只修改学生月度结算统计口径。
- 月度结算统计与课时管理顶部统计统一：
  - 预定课时：所有预定课时都计入，包括取消课。
  - 预定课时费：所有预定课时按“单价 × 预定时长”计入，包括取消课。
  - 实际课时：实际课时 + 待补课且计费的预定课时。
  - 实际课时费：计费实际课时 + 待补课且计费的预定课时。
  - 取消课、不计费补课不计入实际侧。
- 月度结算明细表仍显示真实预定/实际记录，不伪造实际课时行。
- 结算确认/锁定区域同步使用新统计口径。
- 本版无 SQL 修改。

### v9.0.1
- 修复 v9.0 模块化版入口脚本没有正确替换的问题。
- 原因：
  - 原 HTML 中的脚本路径是 `./app.js?v=...`
  - v9.0 的替换规则只匹配了 `app.js?v=...`
  - 导致旧 `app.js?v=8.8.13.2` 仍然被加载，模块化文件没有按预期接管。
- 本版修复：
  - 删除旧的 `./app.js?v=8.8.13.2` 引用
  - 正确加载 `js/legacy-core.js`
  - 正确加载 `js/modules/*.js`
  - 最后加载轻量 `app.js?v=9.0.1`
- 功能逻辑仍保持 v8.8.13.2 不变。
- 本版无 SQL 修改。

### v9.0
- 第一阶段 JS 模块化整理版。
- 基于已测试通过的 v8.8.13.2 制作。
- 功能逻辑保持 v8.8.13.2 不变，优先保证稳定性。
- 拆分后的文件结构：
  - `js/legacy-core.js`：当前已测试通过的主体逻辑
  - `js/modules/form-save-fix.js`：保存/登录 event 修复
  - `js/modules/lessons.js`：课时管理模块边界
  - `js/modules/lesson-import.js`：完整课时导入模块边界
  - `js/modules/student-settlement.js`：学生月度结算模块边界
  - `js/modules/income.js`：收入记录模块边界
  - `js/modules/expense.js`：支出记录模块边界
  - `js/modules/reimbursements.js`：报销管理模块边界
  - `app.js`：轻量启动入口和版本标识
- 本阶段不重写课时管理渲染，不改变画面和业务逻辑。
- 后续修改按模块进行，不再在单一 app.js 末尾继续叠加补丁。
- 本版无 SQL 修改。

### v8.8.13.2
- 基于 v8.8.13 稳定版制作。
- 不修改课时管理画面输出。
- 不修改课时排序。
- 修复新增课时保存时报错：
  - `Cannot read properties of undefined (reading 'preventDefault')`
- 原因：
  - v8.8.10 的保存校验包装函数没有把 event 继续传给旧版 `saveForm(e)`。
- 本版修复保存函数链路：
  - 基础 `saveForm(e)` 支持 event 兜底。
  - v8.8.10 校验包装函数改为 `saveForm(e)` 并继续传递 event。
  - v8.8.11 包装函数在无 event 时补安全 event。
- 本版无 SQL 修改。

### v8.8.13
- 修复课时管理顶部“预定课时费合计”仍然排除取消课的问题。
- 新口径：
  - 预定课时费合计 = 所有预定课时的“课程单价 × 预定时长”
  - 包含取消课
- 实际课时合计 / 实际课时费合计维持 v8.8.12 口径：
  - 实际课时按实际记录计算
  - 待补课且计费的预定课时计入
  - 取消课不计入
  - 不计费补课不计入
- 修复同一日期/同一周课程出现回数倒序的问题。
- 排序规则加入 `lesson_count`：
  - 同日期、同老师、同科目下，按回数从小到大排列。
- 本版无 SQL 修改。

### v8.8.12
- 修改课时管理顶部统计口径。
- 预定课时合计：所有预定课时的时长合计。
- 实际课时合计：
  - 实际课时按实际记录时长计算。
  - 待补课且计费的预定课时，也计入实际课时合计。
  - 取消课不计入。
- 预定课时费合计：
  - 按计费预定课时的“课程单价 × 预定时长”计算。
  - 不计费课时不计入。
- 实际课时费合计：
  - 计费实际课时按“课程单价 × 实际时长”计算。
  - 待补课且计费的预定课时按“课程单价 × 预定时长”计入。
  - 不计费补课、取消课不计入。
- 继续隐藏旧的“取消/放假数量”统计。
- 本版无 SQL 修改。

### v8.8.11
- 修复收入记录中“学费未选择学生”时，没有提示而直接回到系统主页的问题。
- 原因：
  - v8.8.10 只在 `saveForm` 外层做校验。
  - 部分保存按钮 / submit 路径没有进入该拦截分支。
- 本版改为三层校验：
  - 保存函数校验
  - 表单 submit 校验
  - 保存按钮 click 校验
- 收入分类为“学费 / tuition / 授業料”时，必须指定学生。
- 未指定学生时：
  - 不保存
  - 不关闭弹窗
  - 不跳转页面
  - 显示错误提示
  - 学生选择框高亮
- 本版无 SQL 修改。

### v8.8.10
- 完整课时导入支持新模板的“计费”列。
  - 是：计费
  - 否：不计费
- 课时状态增加“取消课”。
- 完整课时导入状态规则：
  - 已上课：有上课日期时生成实际课时，默认计费
  - 待补课：不生成实际课时，默认计费
  - 已补课：有上课日期时生成实际课时，默认不计费
  - 取消课：不生成实际课时，默认不计费
  - 如果模板填写了“计费”列，则以计费列为准
- 新增/编辑课时页面状态下拉同步增加“取消课”。
- 收入记录中，如果分类是“学费”，必须指定学生，否则报错。
- 修复月度结算下方“调整原因”无法删除到空白的问题。
  - 这不是刻意设置，而是预览刷新时回填导致。
- 本版无 SQL 修改。

### v8.8.9
- 修复学生月度结算没有计入跨月补课的问题。
- 月度结算同步课时管理状态规则：
  - 已上课 / completed：计入实际课时
  - 已补课 / makeup_completed：计入实际课时
  - 待补课 / pending_makeup：不计入实际课时
- 月度结算继续使用 `year_month` 作为结算归属月份。
  - 例如 3 月预定课，4 月补上，但 `year_month = 2026-03`，就计入 3 月结算。
- 结算确认 / 锁定区域的计算也同步使用同一套实际课时筛选规则。
- 新增调试函数 `debugSettlementLessonsV889(studentId, month)`。
- 本版无 SQL 修改。

### v8.8.8
- 修复完整课时导入后页面显示混乱的问题。
- 原因：Excel 的时间单元格被读取成 1899-12-30 的 Date 对象，页面直接显示了很长的 GMT 时间文本。
- 完整课时导入时将 Excel 时间统一转换为 HH:MM。
- 页面显示实际课时时间时，也统一清理异常的 1899/GMT 文本。
- 新增修复函数 `repairLessonTimeStringsV888()`：
  - 如果旧导入数据已经把异常时间字符串保存进数据库，可在 Console 手动执行一次。
- 本版无 SQL 修改。

### v8.8.7
- 课时管理顶部统计中隐藏“取消/放假数量”。
- 完整课时导入支持两种实际上课时间格式：
  - 时间：18:00-20:20
  - 开始时间 / 结束时间 两列
- 实际课时导入时保存：
  - start_time
  - end_time
  - actual_minutes
- 新增 SQL：
  - `school_v8_8_7_actual_minutes.sql`
- 实际课时页面显示上课时间。
- 如果实际侧没有“时长（H）”列，则根据开始时间 / 结束时间自动计算实际时长。
- 自动计算时长保存为精确小时数，后续老师工资结算时再按老师 + 科目 + 月份汇总总分钟数后，统一按 15 分钟向下取整。
- 未选择学生时，“导入完整课时”按钮改为非激活状态，和新增课时、导入 Excel 保持一致。
- 本版有 SQL 修改，需要先执行。

### v8.8.6
- 新增课时回数字段 `lesson_count`。
- 新增 SQL：
  - `school_v8_8_6_lesson_count.sql`
- 完整课时导入时：
  - Excel 的“回数”写入 `lesson_count`
  - 不再把回数作为备注的一部分保存
- 重复预定判断加入 `lesson_count`：
  - 回数不同，不再显示重复预定提示
  - 回数相同，才判断为重复
- 课时页面日期区域显示“第X回”。
- 新增/编辑课时页面增加“回数”输入框。
- 未选择学生时：
  - 新增课时不可用
  - 导入Excel不可用
  - 导入完整课时不可用
- 本版有 SQL 修改，需要先执行。

### v8.8.5
- 课时状态统一调整为：
  - 已上课
  - 待补课
  - 已补课
- 新增/编辑课时页面的状态下拉菜单同步改为以上三项。
- 完整课时导入规则调整：
  - 读取“回数”
  - 读取“备注”
  - 读取“工资结算备注”
  - 上课日期为空时，不生成实际课时
  - 状态为“待补课”时，不生成实际课时
  - 状态为“已上课 / 已补课”且有上课日期时，生成实际课时
  - 实际课时的 year_month 使用预定课时月份
  - 同一行预定和实际强制一一对应
- 导入备注中保留：
  - 回数
  - 备注
  - 工资结算备注
- 导入确认弹窗增加“未生成实际课时”数量。
- 本版无 SQL 修改。

### v8.8.4
- 调整课时管理初始月份：
  - 进入课时管理时，如果月份为空，自动填入当前月份。
- 保持课时管理初始列表显示全部学生数据。
  - 导入类操作仍然必须先选择学生，避免误导入。
- “导入完整课时”改为弹窗式导入：
  - 可拖入 Excel 文件
  - 也可点击按钮选择文件
- 修复完整课时导入确认后报错：
  - null value in column "id" of relation "school_lesson_records" violates not-null constraint
- 原因：
  - 完整课时导入时只给预定课时生成了 id
  - 实际课时没有显式 id，而当前表 id 没有数据库默认值
- 本版改为预定课时和实际课时都显式生成 UUID。
- 本版无 SQL 修改。

### v8.8.3
- 修复 v8.8.2 中报销状态判断递归导致页面报错的问题。
- 报错内容：Maximum call stack size exceeded。
- 原因：v8.8.2 中报销识别函数互相覆盖后造成无限递归。
- 本版重新定义不递归的报销支出识别逻辑。
- 支出页面继续根据报销-支出关联表显示“已报销”。
- 保留旧数据兼容判断。
- 新增调试函数 `debugReimbursementLinksV883()`。
- 本版无 SQL 修改。

### v8.8.2
- 新增报销-支出关联表 `school_reimbursement_expenses`。
- 修复报销记录和支出记录之间没有稳定关联的问题。
- 生成报销时，会同时写入报销-支出关联表。
- 删除报销时，会清除对应关联。
- 待报销支出列表会排除已经关联到有效报销记录的支出。
- 支出记录页面根据关联表显示“已报销”。
- 保留旧字段/旧逻辑兼容判断。
- 新增调试函数 `debugReimbursementLinksV882()`，可在 Console 查看报销关联和已报销支出 ID。
- 新增 `school_v8_8_2_reimbursement_links.sql`，需要先执行。
- 主界面版本号完整显示为 v8.8.2。

### v8.8.1
- 修复支出重新报销后，支出记录状态仍不显示“已报销”的问题。
- 原因：
  - 报销流程不一定把 reimbursement_id / reimbursed_at 写回支出记录本身。
  - 关联关系可能保存在报销记录或报销明细中。
- 本版改为多路径判断支出是否已报销：
  - 支出自身字段
  - 报销记录中的 expense_ids / expenses
  - 报销明细记录中的 expense_id
- 支出列表渲染后会自动补显示“已报销”标识。
- 增加调试函数 `debugReimbursedExpenseIdsV881()`，可在 Console 查看系统识别到的已报销支出 ID。
- 本版无 SQL 修改。

### v8.8
- 新增“导入完整课时”功能。
- 用于导入过去已经完成月份的课时记录。
- 一次导入会同时生成预定课时和实际课时。
- 同一行导入的预定课时和实际课时会自动用 planned_lesson_id 建立关联。
- 导入前会显示确认信息：学生、文件名、预定课时条数、实际课时条数、实际课时费合计、跳过行数。
- 导入批次继续支持“撤回本次导入”。
- 支出记录中，如果支出已经报销，状态区域追加显示“已报销”。
- 主界面版本号完整显示为 v8.8。
- 本版无 SQL 修改。

### v8.7.6
- 小修结算确认 / 锁定区域的调整金额输入框。
- 选择“手动调整”时，调整金额输入框不再自带 0，改为空白。
- 选择“结转到下月”时，调整金额自动为 0。
- 选择“抹平差额”时，调整金额自动填入系统差额的相反数。
- 本版无 SQL 修改。

### v8.7.5
- 再次修复结算锁定 RLS 报错。
- 原因：
  - v8.7.4 只给 authenticated 用户配置了结算锁定表权限。
  - 当前前端直连 Supabase 的运行角色可能是 anon。
  - 所以即使执行了 v8.7.4 SQL，insert 仍然会被 RLS 拦截。
- 新增 `school_v8_7_5_rls_anon_fix.sql`：
  - 允许 anon / authenticated 读取结算锁定记录
  - 允许 anon / authenticated 新增结算锁定记录
  - 允许 anon / authenticated 更新结算锁定记录
  - 允许 anon / authenticated 删除测试结算锁定记录
  - 同时补充 GRANT 权限
- 本版只修 RLS 权限，不改业务逻辑。
- 主界面版本号完整显示为 v8.7.5。

### 更早版本
- 旧版详细更新记录已从 README 中省略。
- 如需查看历史改动，请通过 GitHub commit history 查询。
