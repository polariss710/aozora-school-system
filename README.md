# 青空进学塾运营管理系统

## 更新记录（倒序）



### v4.0
- 修复 `renderAttachmentLinks is not defined` 导致支出记录无法渲染的问题。
- 强制将 app.js / style.css 版本参数更新为 v4.0，避免浏览器继续使用 v3.8 缓存。
- 支出记录读取时确认包含附件关联数据。
- 本版无 SQL 修改。

### v3.9
- 修复凭证读取/新增支出保存后，因为月份筛选仍停留在当前月份，导致新记录看不到的问题。
- 保存收入/支出后，自动把对应页面的月份筛选切换到该记录的归属月份。
- 凭证读取识别到日期后，也会提前切换支出月份筛选。
- app.js / style.css 版本参数更新为 v3.9。
- 本版无 SQL 修改。

### v3.8
- 整理 README 更新记录格式。
- 去掉每个版本前重复出现的标题，只保留一个总标题和一组倒序更新记录。
- app.js / style.css 版本参数更新为 v3.8。
- 本版无 SQL 修改。

### v3.7
- 修复图片凭证上传到 Supabase Storage 时因中文/特殊字符文件名导致 400 Bad Request 的问题。
- 上传到 Storage 的文件名改为安全的 ASCII 文件名，但记录里仍保留原始文件名。
- 增加 Storage 上传失败时的调试信息。
- app.js / style.css 版本参数更新为 v3.7。
- 本版无 SQL 修改。

---

### v3.6
- 修复 v3.5 中残留独立 `async` 语句导致的 `async is not defined` 报错。
- 重新校正删除筛选收入 / 删除筛选支出函数声明。
- app.js / style.css 版本参数更新为 v3.6。
- 本版无 SQL 修改。

---

### v3.5
- 修复 v3.4 中 `await is only valid in async functions` 的语法错误。
- 删除筛选收入 / 删除筛选支出的函数恢复为 async。
- app.js / style.css 版本参数更新为 v3.5。
- 本版无 SQL 修改。

---

### v3.4
- 收入记录同步支出记录的体验改动。
- 收入记录按归属月份分组显示。
- 收入记录月份筛选默认当前月份。
- 收入记录增加“删除筛选收入”按钮，可一次删除当前筛选条件下的收入记录。
- 删除收入时同步还原账户余额。
- 收入记录日期显示增加 fallback：如果 `income_date` 为空，则显示创建日期。
- 本版无 SQL 修改。

---

### v3.3
- 修复 `renderAttachmentLinks is not defined` 导致支出记录无法显示的问题。
- 恢复支出记录列表中的凭证链接显示。
- app.js / style.css 版本参数更新为 v3.3。
- 本版无 SQL 修改。

---

### v3.2
- 支出记录按归属月份分组显示。
- 支出记录月份筛选默认当前月份，避免日期筛选框一直空白不直观。
- 支出记录增加“删除筛选支出”按钮，可一次删除当前筛选条件下的支出记录。
- 删除支出时同步还原账户余额。
- 支出记录日期显示增加 fallback：如果 `expense_date` 为空，则显示创建日期。
- 本版无 SQL 修改。

---

### v3.1
- 支出记录支持 PDF / JPG / PNG 凭证读取。
- 图片凭证使用 Tesseract.js OCR 尝试识别日期、金额、说明。
- 新增 Regus / IWG Japan 发票模板。
- 新增 ラクスル 印刷费领收书模板。
- 新增 青空教育株式会社行政手续类凭证模板。
- 保存支出时自动上传原始文件到 Supabase Storage。
- 支出记录新增凭证列，可点击下载/打开原始文件。
- 新增 `school_v3_1_migration.sql`，需要先执行。

---

### v3.0
- 强制覆盖首页与收支汇总的金额统计函数。
- 统计逻辑固定为：按每条记录的 `currency` 与 `amount` 计算，不再读取 `amount_cny` / `amount_jpy` 作为汇总主口径。
- 修复 JPY 记录仍显示成 27.49 这类美金金额的问题。
- app.js / style.css 版本参数更新为 v3.0。
- 本版无 SQL 修改。

---

### v2.9
- 直接重写收支汇总计算逻辑。
- JPY 记录按 `amount` 汇总，CNY 记录按 `amount` 汇总，不再使用 `amount_cny` 作为主统计口径。
- 修复 JPY 支出显示为 `¥27.49` 的问题。
- app.js / style.css 版本参数更新为 v2.9。
- 本版无 SQL 修改。

---

### v2.8
- 修复收支汇总金额口径错误。
- 原来汇总只看 `amount_cny`，导致 JPY 支出如果没有汇率会被算成 0；有 USD/JPY 汇率时还会显示成 27.49 这类金额。
- 现在收支汇总按币种分别统计 JPY / CNY。
- 首页本月收入/支出/结余也按币种分别显示。
- 本版无 SQL 修改。


---

### v2.7
- 修复 PDF读取提示识别到金额，但新增支出弹窗金额仍显示 0 的问题。
- openCreateModal 支持预填数据，并在 PDF读取后再次强制把识别结果写入弹窗字段。
- app.js / style.css 版本参数更新为 v2.7。
- 本版无 SQL 修改。

### v2.6
- 修复 PDF读取后金额没有自动填入的问题。
- 金额识别支持 `¥` / `￥` / `JP¥`，并在多个金额中优先取合计金额。
- PDF读取完成提示中显示识别金额。
- 备注中加入读取文本片段，方便排查新模板。
- app.js / style.css 版本参数更新为 v2.6。
- 本版无 SQL 修改。

### v2.5
- 修复支出记录「PDF读取」按钮点击无反应的问题。
- 确保支出页面存在 PDF读取按钮、隐藏文件选择框和 PDF.js 读取逻辑。
- app.js / style.css 版本参数更新为 v2.5，避免缓存旧脚本。
- 本版无 SQL 修改。

### v2.4
- 给 app.js / style.css 加版本参数，强制浏览器和 GitHub Pages 使用最新脚本，避免 index 已更新但 app.js 仍是旧缓存。
- buildForm 改用 v2.4 专用字段函数别名，避免 getFields 缓存/缺失继续报错。
- 左侧版本号更新为 v2.4。
- 本版无 SQL 修改。

### v2.3
- 强制修复 `getFields is not defined`，确保新增/编辑弹窗可以打开。
- 修正左侧菜单版本号显示为 v2.3。
- README 改为倒序记录。
- 本版无 SQL 修改。

### v2.2
- 修复 v2.1 中 `getFields` 缺失导致新增/编辑弹窗打不开的问题。
- 本版无 SQL 修改。

### v2.1
- 支出记录增加 PDF读取按钮。
- 支持浏览器内读取 PDF 文本。
- 支持いいオフィス领收书模板。
- 支持 Genspark / MainFunc 收据模板。
- 读取后只打开支出新增预览，不会自动保存。
- 本版无 SQL 修改。

### v2.0
- 重写收入表单字段配置。
- 修复收入分类为“学费”时学生下拉框不显示的问题。
- 本版无 SQL 修改。

---

## 使用方法
1. 解压全部文件。
2. 覆盖 GitHub Pages 仓库根目录文件。
3. 提交并 push。
4. 浏览器强制刷新页面。


## 旧版说明

---

## 使用方法

1. 解压全部文件。
2. 覆盖 GitHub Pages 仓库根目录文件。
3. 如 zip 中包含 `*_migration.sql`，先在 Supabase SQL Editor 执行。
4. 提交并 push。
5. 浏览器强制刷新页面。

## 注意

- 没有 SQL 修改的版本，zip 中不会包含旧 SQL 文件。
- 支出凭证上传功能需要先执行 v3.1 的 migration SQL。
