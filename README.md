# 青空进学塾运营管理系统

## 更新记录（倒序）

### v2.8
- 修复收支汇总金额口径错误。
- 原来汇总只看 `amount_cny`，导致 JPY 支出如果没有汇率会被算成 0；有 USD/JPY 汇率时还会显示成 27.49 这类金额。
- 现在收支汇总按币种分别统计 JPY / CNY。
- 首页本月收入/支出/结余也按币种分别显示。
- 本版无 SQL 修改。


---

# 青空进学塾运营管理系统

## 更新记录（倒序）

### v2.7
- 修复 PDF读取提示识别到金额，但新增支出弹窗金额仍显示 0 的问题。
- openCreateModal 支持预填数据，并在 PDF读取后再次强制把识别结果写入弹窗字段。
- app.js / style.css 版本参数更新为 v2.7。
- 本版无 SQL 修改。

### v2.6
- 修复 PDF读取后金额没有自动填入的问题。
- 金额识别支持 `¥` / `￥` / `JP¥`，并在多个金额中优先取合计金额。
- PDF读取完成提示中显示识别金额。
- 备注中加入读取文本片段，方便排查新模板。
- app.js / style.css 版本参数更新为 v2.6。
- 本版无 SQL 修改。

### v2.5
- 修复支出记录「PDF读取」按钮点击无反应的问题。
- 确保支出页面存在 PDF读取按钮、隐藏文件选择框和 PDF.js 读取逻辑。
- app.js / style.css 版本参数更新为 v2.5，避免缓存旧脚本。
- 本版无 SQL 修改。

### v2.4
- 给 app.js / style.css 加版本参数，强制浏览器和 GitHub Pages 使用最新脚本，避免 index 已更新但 app.js 仍是旧缓存。
- buildForm 改用 v2.4 专用字段函数别名，避免 getFields 缓存/缺失继续报错。
- 左侧版本号更新为 v2.4。
- 本版无 SQL 修改。

### v2.3
- 强制修复 `getFields is not defined`，确保新增/编辑弹窗可以打开。
- 修正左侧菜单版本号显示为 v2.3。
- README 改为倒序记录。
- 本版无 SQL 修改。

### v2.2
- 修复 v2.1 中 `getFields` 缺失导致新增/编辑弹窗打不开的问题。
- 本版无 SQL 修改。

### v2.1
- 支出记录增加 PDF读取按钮。
- 支持浏览器内读取 PDF 文本。
- 支持いいオフィス领收书模板。
- 支持 Genspark / MainFunc 收据模板。
- 读取后只打开支出新增预览，不会自动保存。
- 本版无 SQL 修改。

### v2.0
- 重写收入表单字段配置。
- 修复收入分类为“学费”时学生下拉框不显示的问题。
- 本版无 SQL 修改。

---

## 使用方法
1. 解压全部文件。
2. 覆盖 GitHub Pages 仓库根目录文件。
3. 提交并 push。
4. 浏览器强制刷新页面。


## 旧版说明

# 青空进学塾运营管理系统 v1.1

## 本版包含
- Supabase school project 连接
- 首页统计
- 业务归属管理
- 学生管理
- 老师管理
- 科目管理
- 账户管理
- 基础资料 JSON 备份导出

## 使用方法
1. 解压全部文件。
2. 用浏览器打开 `index.html`。
3. 或上传到 Netlify / Vercel / GitHub Pages 等静态托管。
4. 目前 RLS 还是开发开放状态，不建议公开部署链接。

## 下一版计划
- 学生月度课时表
- 预定课时与实际课时
- 学费结算


## v1.1 修改
- 整体色调改为蓝色
- 添加横版/竖版 logo
- 老师所属改为下拉菜单：常勤老师、バイト老师、事务老师
- 老师状态独立为：在职、休假、暂停、离职、停用


## v1.2 修改
- 科目分类改为下拉菜单：学部进学、大学院进学、资格考试对策、特殊课程
- 科目颜色增加常用色块，可点击快速选择，也保留自定义取色器


## v1.3 修改
- 科目管理一览中的颜色列只显示色块，不再显示颜色代码


## v1.4 修改
- 科目管理一览右侧增加拖动排序区域，拖动后自动保存 sort_order
- 科目分类扩展为三级：一级分类、二级分类、三级分类
- 一级分类：班课 / VIP
- 二级分类：学部进学 / 大学院进学 / 资格考试对策 / 特殊课程
- 三级分类根据二级分类自动变化
- 新增 school_v1_4_migration.sql，请先在 Supabase SQL Editor 执行


## v1.5 修正
- 修复科目编辑按钮报错：三级分类读取编辑数据时的变量作用域问题
- 优化科目拖动排序：仅拖动右侧拖动按钮，减少无效保存和界面重绘
- 本地 file:// 打开时不再加载 manifest / service worker，避免 CORS 控制台警告


## v1.6 修改
- 新增收入记录页面
- 新增支出记录页面
- 新增公司收支月度汇总页面
- 收入/支出保存、修改、删除时联动账户 current_balance
- 同步写入 school_account_transactions 账户流水
- 首页增加本月收入、本月支出、本月结余


## v1.7 修改
- 收入记录中，收入分类为“学费”时显示学生选择框
- 学费收入可关联学生管理中的学生资料，并保存在 school_income_records.student_id
- 收入一览增加学生列
- 收入分类不是学费时自动清空学生关联


## v1.8 修正
- 修复收入分类为“学费”时学生选择框不显示的问题
- 学费收入学生选择框改为专用显示逻辑，避免被通用条件字段逻辑影响
- 本版无 SQL 修改


## v1.9 修正
- 强制修复新增收入中「学费」分类下学生下拉框不显示的问题
- 收入一览保留学生列
- 将「公司收支」菜单改名为「收支汇总」
- 本版无 SQL 修改


## v2.0 修正
- 重写 getFields 与 renderField，彻底修复收入分类为学费时学生选择框不显示的问题
- 新增收入中学生选择框会在收入分类=学费时显示，切换到其他分类时隐藏并清空
- 本版无 SQL 修改


## v2.1 修改
- 支出记录增加 PDF读取按钮
- 使用 PDF.js 在浏览器读取 PDF 文本
- 支持いいオフィス领收书模板，自动识别教室费、日期、金额、说明
- 支持 Genspark/MainFunc 收据模板，自动识别软件订阅费、日期、日元扣款金额、汇率信息
- 读取后只打开支出新增预览，不会自动保存；确认后再保存并联动账户
- 本版无 SQL 修改


## v2.2 修正
- 修复 v2.1 中 getFields 未定义导致新增/编辑弹窗打不开的问题
- 本版无 SQL 修改
